# Dependencies
node_modules/

# SQLite: carpark.db is committed intentionally so deploy matches repo (re-add carpark.db here if you want it local-only).
*.db-journal

# Environment variables (contains secrets)
config.env
.env

# Cookies / test files
cookies.txt
cookies2.txt
cookies_new.txt
final_cookies.txt
staff_cookies.txt
test_cookies.txt

# Misc
nul
*.log

# Temporary DB migration artifacts
carpark-old*
carpark-b64-*
export-old.err

# Local DB exports (e.g. Railway pull) — do not commit
backups-railway/
